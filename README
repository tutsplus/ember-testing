ember testing example showing:

1.) unit tests for model computed property
2.) integration tests to verify bound models render correctly

To run the javascript tests with karma

    npm install
    ./node_modules/karma/bin/karma start


To run the ember.js web app

    npm install
    export PATH="./node_modules/.bin:$PATH"

    ./bin/server
    http://localhost:3000
